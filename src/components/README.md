# src/components

Place shared, reusable UI components here.
Page-specific components can live alongside their page files in `src/pages/`,
while truly shared components (buttons, modals, cards, etc.) belong here.
